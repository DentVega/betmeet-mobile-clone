# Active Context

> **Agent note:** This is your short-term memory. Read it at the start of every session and update it immediately after making an important decision, changing focus, or encountering a blocker.

## Current Focus
- **Intent 002 (v2 full web parity) — Inception COMPLETE.** Gap analysis (web Units 1–72 vs mobile v1) done; all 5 artifacts written + approved under `memory-bank/intents/002-v2-web-parity/`. Scope: full parity incl. 3 native modules (image picker, passkeys/biometrics, push FCM/APNs); auto-scoring + minimal result-entry (NO live football-data sync / full admin); single bundle. 8 units, 33 stories, 10 bolts (V1–V10). **Next: `/bolt-start` for V1 (i18n foundation).**

## Previous Focus (Intent 001 + design)
- **🏁 v1 FEATURE-COMPLETE — all 10 bolts (0–9) done, validated locally** (tsc clean, jest 51/51, Rspack bundle green; all SQL/functions exercised on ephemeral PG17). Remaining work is **activation**, not features.
- **Backend FULLY DEPLOYED** to the user's Supabase (verified): 10/10 migrations applied, 8/8 Edge Functions ACTIVE, ADMIN_SECRET set. Remaining = client-side activation only: **native rebuild for FlashList** (`pod install` + run) and **dashboard config** (Auth redirect URLs `betmeet://…` + public `avatars` bucket). Then device E2E.
- Pending dashboard (manual): Auth redirect URLs `betmeet://...` + public `avatars` bucket (for full auth E2E + avatar images).
- Architecture (ADR-007): own Supabase backend, RLS reads + Edge Function writes, mobile-direct, no Next.js. Blueprint = `../betmeet-clone`.
- Inception re-scoped: `requirements.md`, `system-context.md`, `bolt-plan.md` updated. New phase order: Phase A (shell/auth/audit) ✅ → **Phase B (backend: schema+RLS → Edge Functions → match seed)** → Phase C (mobile onboarding/predictions/pools/leaderboard, blocked on B).
- Bolts 0/1/2 done (3/9). Env wiring done (`.env` → DefinePlugin). `profileGate` corrected to `profiles`/`onboarding_completed`.

## Recent Technical Decisions
- **Single bundle, host-only — no Module Federation in v1.** Remote candidates (Matches, Pools, Leaderboard) recorded for later; do not `/repack-init` until reversed.
- **v1 scope:** Auth (email + Google + verify/reset; Passkeys/MFA → v2), Onboarding (nickname + default-avatar gate; custom upload → v2), Matches & Predictions, Pools, Leaderboard. Settings, push, biometrics, Admin → out.
- **Native:** secure token storage (mandatory) + deep links (`betmeet://` for email confirm/reset + pool join). No image picker, no push in v1.
- **Backend frozen.** Mobile is a pure Supabase/PostgREST client.
- Still **unchosen** (Bolt 0 ADRs): navigation library, data-fetching/cache library, secure-storage library, custom-scheme vs Universal/App-Links.

## Recent Fixes (FIRE)
- **Pull-to-refresh on data-backed tab roots (2026-07-14) — 0-checkpoint FIRE.** Swipe-down manual refresh added to **Matches**, **Pools (PoolsList)**, **Rankings** (the three server-data tab roots). New shared hook `src/ui/RefreshControl.tsx` (`useRefreshControl({ refetch, refreshing })`) returns a theme-tinted `<RefreshControl>` element (tintColor/colors/progressBackgroundColor from `useTheme()`), passed to each FlashList via the `refreshControl` prop (FlashList v2 forwards it as a ScrollView prop; works for ScrollView too). Wired `refreshing={isRefetching}` + `onRefresh={refetch}` from the existing react-query hooks (useFixture/useMyPools/useGlobalRankingLive) — no hand-rolled state. **Skipped (intentional):** Rules (static, nothing to refetch), Settings root (local prefs), ProfileScreen (server data but a screen *over* the tabs, not a tab, and already invalidates on mutation). No new deps, no i18n strings. tsc clean, jest 77/77 (new `src/ui/__tests__/RefreshControl.test.tsx` via react-test-renderer — RNTL is NOT installed here). **Follow-ups:** (1) empty-state Matches/Pools/Rankings render a non-scrollable `<Txt>`, so pull-to-refresh is unavailable when the list is empty — wrap empties in a refreshable ScrollView if desired; (2) ProfileScreen is a 2-line add with the same hook if wanted; (3) add RNTL + agent-device smoke test for real per-screen render + device pull gesture.
- **User-selectable timezone (2026-07-14) — 1-checkpoint FIRE.** Match dates/times now honour a user-picked IANA zone (Settings), with device tz as the DEFAULT (fixes wrong-emulator-tz). New `src/settings/timezoneStore.ts` (zustand + secureStorage + `hydrate()`, mirrors localeStore) exposes `useTimezone()` (returns effective tz, stable primitive). Replaced the 3 module-level `const TZ` (MatchCard, MatchesScreen, useFixture) with the reactive hook; `useFixture` now returns raw `FixtureMatch[]` from the query and groups client-side in a `useMemo` keyed on tz (re-buckets days on tz change, NO network refetch). UI: `src/ui/TimezoneSwitcher.tsx` (curated zones incl. America/La_Paz + "Device default"), added to SettingsScreen. i18n `settings.timezone.*` in en+es. Hydrated in App.tsx. MatchCard stays memoized (tz is a stable primitive prop). tsc clean, jest 74/74 (extended fixture.test.ts w/ La_Paz/UTC/Madrid + day-boundary cases; new timezoneStore.test.ts). **Follow-up:** only Matches renders kickoff times today — if future screens (pool detail, notifications) show match times, route them through `useTimezone()` too. Optional: sync the override to `profiles` like locale does.
- **Matches "today's France vs Spain doesn't show" (2026-07-14) — DATA fix, 0-checkpoint FIRE.** Root cause: the FRA–ESP match was never seeded, and the whole dev fixture (`20260630140000`) predates today (latest kickoff 2026-07-10), so the Matches list (hides past days) rendered empty. Domain logic (`fixture.ts`) was correct. Fix = additive idempotent migration `supabase/migrations/20260714120000_seed_france_spain_today.sql` (FRA vs ESP R16, kickoff `now()`-derived so it's always today) + regression tests in `src/matches/__tests__/fixture.test.ts` (10/10 pass). **User action pending:** `supabase db push` to apply on remote (seed changes don't touch the live DB).

## Known Issues / Blockers
- **TOP RISK:** web mutations are Next.js **server actions**, not callable from mobile. Bolt 2 (Write-Path Audit) must map each v1 write to Supabase SDK / PostgREST+RLS / needs-a-thin-endpoint, and surface any backend exception to the user. Gates Onboarding/Predictions/Pools.

## Immediate Next Step
- **Activate v1** (see progress.md "Activation checklist"): `supabase db push`; `supabase functions deploy set-nickname leave-pool kick-member delete-pool`; `pod install` + native rebuild (FlashList); dashboard redirect URLs + `avatars` bucket; optionally `compute-score` on the 3 FINISHED seeded matches.
- Then **device E2E** (agent-device): signup→verify→onboarding→predict→pool join (deep link)→leaderboard. After that, candidate v2 work: live projection, custom avatar upload, push, Passkeys/MFA, Settings, Admin, directed invites.
