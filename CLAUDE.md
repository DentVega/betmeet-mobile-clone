# CLAUDE.md

> Project memory loaded every session. Generated by the rn-repack-aidlc plugin.
> Fill the `[PROJECT]` blanks once; keep this file in sync with `memory-bank/standards/`.

## Project
- **Name:** `BetmeetMobile` (`betmeet-mobile-clone`)
- **What it is:** React Native + Re.Pack mobile app — AI-DLC workspace initialized under tag `prueba`.
- **Platforms:** iOS / Android

## Stack (authoritative — see memory-bank/standards/tech-stack.md)
- **Framework:** React Native 0.86 (New Architecture) + React 19.2.
- **Bundler:** **Re.Pack 5.2** (Rspack) — **NOT Metro**. Never generate Metro config. **Single bundle today; Module Federation deliberately deferred.**
- **Language:** TypeScript (strict).
- **Lists:** FlashList. **Navigation:** native navigators. **State:** not yet chosen (decide on first stateful bolt).
- **Federation:** **single bundle — no Module Federation for now (deliberate).** Scaffold host + remote chunks via `/repack-init` only when an on-demand-chunk need is confirmed. Topology lives in `memory-bank/standards/system-architecture.md`.

## How to work here (AI-DLC)
- Pick a flow by size: `/simple-spec` (small), `/fire` (medium, brownfield), AI-DLC `/aidlc-inception` → `/bolt-start` → `/operations` (large/new remote). Unsure → `/spec-flow`.
- All work traces to a story → unit → intent under `memory-bank/`.

## Skill precedence (project wins)
1. If an installed skill contradicts this file or `memory-bank/standards/`, **this project wins**.
2. If a skill complements it (e.g. an FPS optimization), follow it.
3. If a skill's advice breaks an architectural rule (federation boundaries, native-module placement), **don't apply it silently** — raise it.

## Performance skills — when to use which
- Writing RN components → `vercel-react-native-skills`.
- Debugging a measured perf problem → `react-native-best-practices`.
- React-general logic (data fetching, re-renders) → `react-best-practices`.
- Reusable component design → `composition-patterns`.

## Testing
- Components → React Native Testing Library. Device E2E / federated-chunk loading → `agent-device`.

## Docs are bilingual
- This repo keeps `<name>.md` + `<name>.<lang>.md` in sync. Any doc edit must update every language variant (`i18n-doc-sync` skill + `scripts/check-i18n-docs.mjs`).
